!!!
%html
  %head
    = partial "head-contents"

  %body
    %header.article-navbar
      .article-navbar__wrapper.container
        .article-navbar__inner.column
          .article-navbar__left
            %p [LOGO]
          %nav.article-navbar__right
            %ul.article-navbar__links
              %li= link_to "Family", "#"
              %li= link_to "Ministry", "#"
              %li= link_to "Archives", "#"
              %li= link_to "Contact", "#"
              %li= link_to "Donate", "#"

    -# #main{:role => "main"}
    -#   %h1 Article Layout
    -#   = yield
    -# %aside
    -#   %h2 Recent Articles
    -#   %ol
    -#     - blog.articles[0...10].each do |article|
    -#       %li
    -#         = link_to article.title, article
    -#         %span= article.date.strftime("%b %e")
    -#   %h2 Tags
    -#   %ol
    -#     - blog.tags.each do |tag, articles|
    -#       %li= link_to "#{tag} (#{articles.size})", tag_path(tag)
    -#   %h2 By Year
    -#   %ol
    -#     - blog.articles.group_by {|a| a.date.year }.each do |year, articles|
    -#       %li= link_to "#{year} (#{articles.size})", blog_year_path(year)
